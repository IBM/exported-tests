language: node_js

notifications:
  email: false

install: yarn

jobs:
  include:
    - stage: test
      script:
        - yarn lint
        - yarn test
    - stage: build
      script: yarn build
    - stage: pre-deploy
      script: npx semantic-release --dry-run
